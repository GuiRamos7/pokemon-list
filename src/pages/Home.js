import React from "react";
import { Header, PokemonItem } from "components";

const Home = () => {
  return (
    <div>
      <Header />
      <PokemonItem />
    </div>
  );
};

export default Home;
